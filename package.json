{
  "name": "smart-civic-system-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install": "npm install --prefix backend && npm install --prefix frontend"
  }
}
